LOAD DATABASE
     FROM postgresql://postgres:postgres@localhost/postgres
     INTO postgresql://postgres:postgres@db-server/postgres

 WITH include no drop, create tables, create indexes, reset sequences

  SET work_mem to '16MB', maintenance_work_mem to '512 MB';
